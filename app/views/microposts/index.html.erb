<% provide(:title, 'All users feed') %>
<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <h1>All users feed</h1>

    <ol class="microposts">
      <%= render @feedall %>
    </ol>

    <%= paginate @feedall %>
  </div>
</div>